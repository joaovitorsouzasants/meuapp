import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    button: {
        backgroundColor: 'green',
        padding: 15,
        margin: 20,
        alignItems: 'center',
        borderRadius: 9
    },
    buttonText: {
        color: 'white',
        fontSize: 18,
        fontWeight: 'bold'
    },
})